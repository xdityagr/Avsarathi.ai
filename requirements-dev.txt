# The corpus ingest and the one-off scripts. NOT installed in the image —
# these pull in pandas and pdfplumber, which together are larger than the
# entire runtime and are never imported by the API.
-r requirements.txt
pandas>=2.2
openpyxl>=3.1
xlrd>=2.0
pdfplumber>=0.11
selectolax>=0.3
twilio>=9.0
pytest>=8.0
pytest-asyncio>=0.24
